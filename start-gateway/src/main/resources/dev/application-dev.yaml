# ===================================================================
# 网关服务 - 开发环境配置
# ===================================================================

spring:
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
    nacos:
      discovery:
        server-addr: 192.168.100.20:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: 192.168.100.20:8848
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP
  data:
    redis:
      host: 192.168.100.20
      port: 6379
      password: WeEvBhLapEHJMG4JFfFG
      database: 0

sa-token:
  jwt-secret-key: dev-secret-key-start-admin-cloud-2026-04-27

gateway:
  allowed-origins: "http://localhost:9000,http://localhost:5173,http://127.0.0.1:3000"
