# test profile configuration placeholder
# ===================================================================
# 演示服务 - 开发环境配置
# ===================================================================
# 敏感配置已迁移到 Nacos 配置中心
# Nacos DataId: demo-service-dev.yaml
# 本地仅保留 Nacos 连接地址，用于引导加载远程配置
# ===================================================================

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.100.156:8848
        namespace: public
        group: DEFAULT_GROUP
      config:
        server-addr: 192.168.100.156:8848
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP

sa-token:
  jwt-secret-key: dev-secret-key-start-admin-cloud-2026-04-27
